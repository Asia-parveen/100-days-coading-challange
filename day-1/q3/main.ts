let myName: string = "Asia parveen"; // This saves the name

// Shows the name in all small letters
console.log(myName.toLowerCase());

// Shows the name in ALL BIG LETTERS
console.log(myName.toUpperCase());

 // Shows the name with the First Letter Big
 console.log(myName.charAt(0).toUpperCase() + myName.slice(1).toLowerCase());


 

